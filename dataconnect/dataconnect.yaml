specVersion: "v1beta"
serviceId: "ignitehub-data-connect"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "ignitehub-db"
      cloudSql:
        instanceId: "ignitehub-cloud-sql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
